services:
  forge:
    build: .
    restart: unless-stopped
    ports:
      - "8300:8300"
    env_file:
      - .env.direct-server
    volumes:
      - forge-data:/data/forge
      - ./workspace:/workspace

volumes:
  forge-data:
