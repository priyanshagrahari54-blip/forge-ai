# FORGE AI — CONTRIBUTING
Inspect before editing. Keep changes scoped. Preserve contracts. Add regression tests. Never weaken assertions to pass CI. Run relevant tests and git diff checks. Never commit secrets. Document architectural changes. Use descriptive commits. For production changes, verify deployment runtime rather than assuming a successful push means success.
